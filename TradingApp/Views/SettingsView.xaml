<!-- SettingsView.xaml.cs -->
<UserControl x:Class="TradingApp.SettingsView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="clr-namespace:TradingApp"
            Background="{StaticResource AppBackground}"
            Foreground="{StaticResource AppForeground}">

    <ScrollViewer>
        <StackPanel Margin="20" VerticalAlignment="Top">

            <Border Style="{StaticResource SettingsBlock}">
                <StackPanel>
                    <TextBlock Text="Авторизация API" Style="{StaticResource SettingsBlockTitle}" />

                    <TextBlock Text="Finam Token" Style="{StaticResource LabelText}" />
                    <TextBox Text="{Binding fToken, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource SettingsTextBox}" />

                    <TextBlock Text="Tinkoff Token" Style="{StaticResource LabelText}" />
                    <TextBox Text="{Binding tToken, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource SettingsTextBox}" />

                    <TextBlock Text="Finam Client ID" Style="{StaticResource LabelText}" />
                    <TextBox Text="{Binding ClientId, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource SettingsTextBox}" />

                    <Button Content="Сохранить" Click="SaveSettings_Click"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Right" Width="100"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsBlock}">
                <StackPanel>
                    <TextBlock Text="Параметры торговли" Style="{StaticResource SettingsBlockTitle}" />

                    <ComboBox ItemsSource="{Binding Instruments}"
                          SelectedItem="{Binding SelectedInstrument, UpdateSourceTrigger=PropertyChanged}"
                          Style="{StaticResource SettingsComboBox}" />

                    <TextBlock Text="Чувствительность поиска (от 0 до 100)" Style="{StaticResource LabelText}" />
                    <TextBox 
                         Style="{StaticResource SettingsTextBox}" />

                    <TextBlock Text="Уровень фиксации прибыли" Style="{StaticResource LabelText}" />
                    <TextBox
                         Style="{StaticResource SettingsTextBox}" />

                    <TextBlock Text="Нижний предел доли кластера" Style="{StaticResource LabelText}" />
                    <TextBox
                         Style="{StaticResource SettingsTextBox}" />

                    <Button Content="Сохранить" Click="SaveSettings_Click"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Right" Width="100"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>